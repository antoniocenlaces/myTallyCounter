<%- include ('../partials/header.ejs'); %>
<body id="page-top">

  <!-- Page Wrapper -->
  <div id="wrapper">
    <%- include('../partials/sidebar.ejs', {section: 'dashboard',user: user}); %>

    <!-- Content Wrapper -->
    <div id="content-wrapper" class="d-flex flex-column">

      <!-- Main Content -->
      <div id="content">

          <%- include('../partials/topbar.ejs', {user: user}); %>

        <!-- Begin Page Content TODO-->
        <div class="container-fluid">

          <!-- Page Heading -->
          <div class="d-sm-flex align-items-center justify-content-between mb-4">
            <h1 class="h3 mb-0 text-gray-800"><i class="fas fa-edit mr-2"></i>Configuración Empresas de Transporte</h1>
            <!--<a href="#" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm"><i class="fas fa-download fa-sm text-white-50"></i> Generate Report</a>-->
          </div>
          <p class="mb-0 text-gray-600">· Puede añadir una nueva empresa de transporte separándola de las anteriores por ",".</p>
          <p class="mb-0 text-gray-600">· Puede borrar las empresas que no desee que se muestren.</p>
          <p class="mb-0 text-gray-600">· Si en el directorio \public\carrierLogos\ se encuentra una imagen png o jpg con el mismo nombre que la empresa introducida, se mostrará la imagen correspondiente.</p>
          <p class="mb-0 text-gray-800 mt-2"><strong>Importante:</strong> Solo se mostrarán los 15 primeros caracteres en pantalla.</p>
          <hr>
          <% var messages = getMessages() %>
          <% if (messages.error) { %>
            <div class="alert alert-danger col-sm-12"><%= messages.error %></div>
          <% } %>
          <% if (messages.success) { %>
            <div class="alert alert-success col-sm-12"><%= messages.success %></div>
          <% } %>
          <form action="/admin/carrier" method="post">
              <input type="hidden" name="column" value="value_text" />
              
                <div class="form-group mt-4">
                    <label for="<%= settings[0].key %>"><strong><%= settings[0].label %></strong></label>
                    <textarea name="<%= settings[0].key %>" class="form-control" id="<%= settings[0].key %>" rows="5"><%= settings[0].value_text %></textarea>
                </div>
              
              <div class="form-group">
                  <div class="col-md-4 px-0">
                    <button type="submit" class="btn btn-lg btn-primary mb-2"><i class="fas fa-save mr-2"></i>Guardar</button>
                    <button type="button" class="btn btn-lg btn-warning mb-2" onclick="location.href = '\\admin';"><i class="fas fa-truck mr-2"></i>Volver</button>
                    </div>
              </div>
            </form>
          
          </div>
        <!-- /.container-fluid -->

      </div>
      <!-- End of Main Content -->


<%- include ('../partials/footer.ejs'); %>


</body>

</html>
