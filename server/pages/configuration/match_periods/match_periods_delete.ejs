<%- include ('../../partials/header.ejs'); %>

  <body id="page-top">

    <!-- Page Wrapper -->
    <div id="wrapper">
      <%- include('../../partials/sidebar.ejs', {section: 'matchPeriods' }); %>

        <!-- Content Wrapper -->
        <div id="content-wrapper" class="d-flex flex-column">

          <!-- Main Content -->
          <div id="content">
            <%- include('../../partials/topbar.ejs', {user: user}); %>

              <!-- Begin Page Content TODO-->
              <div class="container-fluid">

                <!-- Page Heading -->
                <div class="d-sm-flex align-items-center justify-content-between mb-4">
                  <h1 class="h3 mb-0 text-gray-800"></i>BORRAR PERIODO DE PARTIDO</h1>
                </div>

                <% var messages=getMessages() %>
                  <% if (messages.error) { %>
                    <div class="alert alert-danger col-sm-12">
                      <%= messages.error %>
                    </div>
                    <% } %>
                      <% if (messages.success) { %>
                        <div class="alert alert-success col-sm-12">
                          <%= messages.success %>
                        </div>
                        <% } %>

                          <form action="/configuration/match_periods/delete/" method="post" id="deleteMatchPeriodForm">
                            <div class="form-group row">
                              <label for="matchPeriodDescription" class="col-sm-2 col-form-label">Descripción de la
                                Entrada</label>

                              <div class="col-md-4">
                                <select class="custom-select my-1 mr-sm-2" id="matchPeriodDescription"
                                  name="matchPeriodDescription" required>
                                  <option selected>Seleccione Periodo</option>
                                  <% matchPeriods.forEach((DB_Periodos_conteo)=> { %>
                                    <option value="<%= DB_Periodos_conteo.PeriodoID %>">
                                      <%= DB_Periodos_conteo.Descripcion %>
                                    </option>
                                    <% }) %>
                                </select>
                              </div>
                            </div>

                            <div class="form-group row">
                              <div class="col-md-2"></div>
                              <div class="col-md-4">
                                <a href="#" class="btn btn-lg btn-danger mb-2 btn-block" data-toggle="modal"
                                  data-target="#deleteMatchPeriodModal">
                                  <span class="text">Borrar Periodo</span>
                                </a>
                              </div>
                            </div>
                          </form>

              </div>
              <!-- /.container-fluid -->

          </div>
          <!-- End of Main Content -->


          <%- include ('../../partials/footer.ejs'); %>

  </body>

  </html>